{
  // Compiler options for the JavaScript project
  "compilerOptions": {
    // Define path aliases for cleaner imports
    "paths": {
      "@/*": ["./src/*"], // The "@" alias maps to the "src" folder
    }
  }
}